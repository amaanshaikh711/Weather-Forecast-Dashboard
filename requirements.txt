I inspected weather_dashboard.py to find all external libraries the app uses and created a requirements.txt that includes the needed runtime packages plus a couple of recommended extras (for loading env vars and production deployment). Install with: pip install -r requirements.txt


# Requirements for Weather-Forecast-Dashboard
# Core runtime dependencies used by weather_dashboard.py
dash>=2.0.0
dash-bootstrap-components>=1.0.0
plotly>=5.0.0
pandas>=1.3.0
requests>=2.25.1
pytz>=2021.1

# Recommended (optional) utilities
python-dotenv>=0.21.0    # load API keys from a .env file instead of hardcoding
gunicorn>=20.0.4         # production WSGI server (if deploying to Linux servers)
